import React from 'react';
import ReactDOM from 'react-dom';

ReactDOM.render(
    <React.StrictMode>
        <div>Point of Sale Application</div>
    </React.StrictMode>,
    document.getElementById('root'),
);
